# HANDOFF — 돌봄 로봇 대화 런타임 설계 인계

> # ⚠️ 낡은 문서입니다 — 현재 상태로 읽지 마십시오
> **2026-07-31 착수 시점**에 쓴 문서이고, 그 뒤 티켓 20여 개가 머지됐습니다.
> 아래 본문은 "코드는 전부 스텁이며 실제 로직은 구현되지 않았다"고 말하는데
> **지금은 사실이 아닙니다.** 게이트·침묵 사다리·트리아지·현관·온보딩·보호자
> 알림이 구현되어 실행 경로에 연결돼 있습니다. 다만 **지금 `ai-develop` 의 테스트는
> 초록이 아닙니다** — `PROGRESS.md` §2.0 을 먼저 보십시오.
>
> **지금 상태를 알고 싶다면 [`docs/carebot/PROGRESS.md`](docs/carebot/PROGRESS.md) 를 보십시오.**
> 코드를 읽어야 한다면 [`docs/carebot/READING-ORDER.md`](docs/carebot/READING-ORDER.md) 입니다.
>
> 이 파일을 지우지 않고 두는 이유는 §2 의 경로 정정(zip 의 `ai_chat/` 대
> 실제 `robot/ai_chat/`)처럼 **다른 곳에 남아 있지 않은 기록**이 있기 때문입니다.
> 착수 경위를 확인할 때만 여십시오.
>
> — S15P11E102-297 에서 추가 (2026-08-04)

> 작성 시점: 2026-07-31
> 이 문서는 **일회성 인계 문서**다. 설계 자체는 `CLAUDE.md`(권위)와
> `docs/design/care-bot-design.md`(장문 근거)에 있다. 착수가 끝나면 이 파일은 지워도 된다.
>
> ~~**Claude Code 첫 세션에서 이 문서를 먼저 읽게 한다.** 사용법은 §7.~~
> **위 경고 참고 — 이 지시는 더 이상 유효하지 않습니다.** 첫 세션에는
> `CLAUDE.md`(자동 적재) + `docs/carebot/PROGRESS.md` 를 읽힙니다.

---

## 1. 지금 어디까지 왔나

설계 대화를 통해 **독거노인 능동형 돌봄 로봇의 대화 런타임**을 설계했고, 그 결과로
문서 2개 + 코드 뼈대 13개 파일 + Jira 티켓 13개를 만들었다. 코드는 전부 스텁이며
**실제 로직은 구현되지 않았다.** 배선(그래프 노드·엣지)과 상태 스키마만 동작 가능한 형태다.

기존 `ai_chat/` 패키지에는 이미 STT·LLM·TTS·날씨·의료조회·오디오 어댑터가 구현되어 있다.
이번 작업은 **그 위에 "언제 말할지 / 말할지 여부 / 무엇을 기억할지 / 언제 사람을 부를지"를
얹는 것**이다. 기존 클라이언트를 재구현하지 않는다.

---

## 2. 산출물과 들어갈 위치

### 문서

> **정정(S15P11E102-200 수행 시 확인).** zip 은 `ai_chat/src/bomi_ai_chat/...` 경로로
> 압축되어 있었지만, 실제 레포의 패키지는 `robot/ai_chat/src/bomi_ai_chat/` 이다.
> 루트에서 그대로 풀면 `S15P11E102/ai_chat/` 이라는 잘못된 트리가 생긴다.
> 200번에서 파일을 실제 경로로 옮겨 배치했고, CLAUDE.md §20 도 실제 트리로 정정했다.
> 코드의 import 는 처음부터 `bomi_ai_chat.*` 였으므로 수정이 필요하지 않았다.

| 파일 | 위치 | 성격 |
| --- | --- | --- |
| `CLAUDE.md` | 레포 루트 `S15P11E102/CLAUDE.md` | **대화 런타임의 권위 문서.** 24개 절. 에이전트가 매 세션 읽는다 |
| `care-bot-design.md` | `docs/design/care-bot-design.md` | 장문 한글 근거. 왜 그렇게 결정했는지 |
| `HANDOFF.md` | 레포 루트 (이 파일) | 일회성 인계 |

### 코드 뼈대 (모두 신규 파일, 기존 파일 덮어쓰지 않음)

```
ai_chat/src/bomi_ai_chat/
├── clock.py            신규   주입 가능한 시계 (§5 절대 규칙)
├── policy.py           신규   대화 정책 상수. config.py 와 다름 (§4 주의)
├── state.py            신규   ConvState 스키마 + SpeechProposal
├── graph/              신규 폴더
│   ├── __init__.py
│   ├── build.py            StateGraph 배선만. 로직 없음
│   ├── ingress.py          3개 진입 경로 + note_interaction + door_event
│   ├── gate.py             능동 발화 게이트 (4단 캐스케이드)
│   ├── triage.py           안전 분류 + T1 에스컬레이션
│   ├── context.py          백엔드 문맥 조립 호출 + 인텐트 라우팅
│   ├── handlers.py         7개 핸들러 (전부 스텁)
│   └── output.py           response_shaper + emit
└── jobs/               신규 폴더
    ├── __init__.py
    └── ticks.py            silence_tick / door_watch_tick / outbox_flush / daily_summary_job
```

### 아직 만들지 않은 폴더 (해당 티켓에서 생성)

| 폴더 | 티켓 |
| --- | --- |
| `localstore/` | 202 |
| `backend_client/` | 204 |
| `prompts/` | 204 |
| `notify/` | 202(인터페이스) → 211(채널 구현) |
| `door/` | 208 |

`ai_chat/pyproject.toml` 과 `README.md` 는 이미 있으므로 **수정**하면 된다
(langgraph·APScheduler 의존성 추가, SimClock 사용법 문서화). 200번 완료 조건에 포함.

---

## 3. Jira 현황

- 프로젝트 `S15P11E102`, 이슈 유형 `스토리`
- 티켓 **S15P11E102-200 ~ 212** (13개), 전부 `해야 할 일`
- 전부 활성 스프린트 **`[3주차] 로봇 자율 주행과 개인화 대화`**(id 51397)에 들어가 있음
- **주의: 이 스프린트는 2026-08-02 00:00 KST 종료.** 13개는 몇 주치 작업이므로 스프린트를
  닫으면 전부 다음으로 넘어간다. 번다운을 신경 쓴다면 200~204만 남기고 나머지는 백로그로
  되돌리는 편이 깔끔하다.

| 키 | 제목 | 선행 조건 |
| --- | --- | --- |
| 200 | [AI](chore) 대화 런타임 모듈 구조 재편 및 주입 시계 도입 | — |
| 201 | [BE](db) 로봇 런타임용 스키마 보강 및 pgvector 활성화 | — (200과 병렬) |
| 202 | [AI](chore) 로컬 상태 저장소 및 보호자 알림 발신 큐 | 200 |
| 203 | [BE](api) 대화 문맥 조립 API | 201 |
| 204 | [AI](chat) 반응형 대화 1왕복 연결 및 프롬프트 조립 분리 | 202 + 203 |
| 205 | [AI](audio) 에코 억제 및 barge-in 양보 처리 | 204 |
| 206 | [AI](chat) 능동 발화 게이트 및 스케줄러 연동 | **205 필수** |
| 207 | [AI](chat) 침묵 사다리 및 압축 시계 기반 검증 | 206 |
| 208 | [AI](chat) 현관 이벤트 연동 및 재실 상태 관리 | 206 |
| 209 | [AI](chat) 계약 주도형 대화 구현 (온보딩·재질의) | 203 + 204 |
| 210 | [AI](chat) 안전 트리아지 및 T1 보호자 에스컬레이션 | 207 + 208 |
| 211 | [BE, AI] T2 일일 요약 및 보호자 알림 채널 구현 | 208 |
| 212 | [AI](test) 자연스러움 회귀 세트 및 임계치 실측 튜닝 | 마지막 |

**205가 206보다 먼저인 이유**: 스피커와 마이크가 한 몸통이라 TTS 출력이 마이크로 되돌아온다.
에코를 안 잡고 능동 발화를 테스트하면 **모든 버그 리포트가 실제로는 에코**이고 게이트 버그로
오진하게 된다.

**210을 마지막에 둔 이유**: 208까지 오면 `occupancy`·`rest_state`가 오탐을 이미 걷어낸
상태라 임계치를 훨씬 좋은 조건에서 잡을 수 있다.

### 브랜치 이름

기존 관례는 **GitLab 이슈에서 브랜치 생성** 시의 자동 네이밍이다.

```
S15P11E102-199-be-bin-폴더-gitignore추가
```

→ `{티켓키}-{제목-슬러그}` 형식을 따른다. `feat/...` 같은 형식은 관례에 맞지 않는다.
이 형식으로 만들면 MR과 티켓이 자동 연결된다.
리포지토리: `lab.ssafy.com/s15-webmobile3-sub1/S15P11E102` (Bomi)

---

## 4. 반드시 지킬 결정들 (요약, 상세는 CLAUDE.md)

### 제품 방향

- 정보 제공 봇이 **아니다.** 축은 **안전 / 정서 / 인지** 세 개. 복약·날씨·병원은 그 위에 얹힌다
- 외로움이 1번 문제다. **말벗이 본체**이고 회상 대화가 정서의 핵심 루프
- 비목표: 낙상 감지, 진단, 상담(자해 시 사람에게 넘긴다)
- vision 은 존재하지만 **`RESTING`/`AWAKE` 전이 전용**이다. 프레임·관절·얼굴 특징 저장 금지

### 구조

- **말하지 않기로 결정하는 것도 기능이다.** 게이트가 침묵을 고르면 그래프는 `END`로 직행하며
  `emit`에 도달하지 않는다
- 판단 노드가 생성 노드보다 **위**다. T1이면 인텐트 라우터와 검색을 전부 건너뛴다
- 모든 발화는 `response_shaper`를 통과한다. 예외 없다
- 진입 경로 3개: `user_utterance`(게이트 안 거침) / `proactive` / `door_event`(occupancy 즉시
  반영 후 인사만 게이트)

### 용어 (충돌 주의)

- ERD가 `candidate`를 이미 소유한다(`fact_candidate` = 미확정 사실)
- 제안된 발화는 **`SpeechProposal` / `proposal`** 이라고 부른다. 절대 `candidate` 라고 쓰지 않는다
- 사용자는 **senior**, 가족은 **guardian**(`PRIMARY`/`SECONDARY`)

### 데이터 소유권

| 계층 | 소유 | 내용 |
| --- | --- | --- |
| 사실 | 백엔드 Postgres (기존 ERD 그대로) | `app_user`, `memory`, `care_record`, `fact_candidate`, 동의 |
| 운영 상태 | **로봇 로컬 SQLite** | 제안 큐, `silence_level`, `occupancy`, `last_spoke_at`, **checkpointer**, 캐시 오디오, outbox |

- **checkpointer 를 서버 DB에 두지 않는다.** 매 턴 쓰기가 일어나 지연 예산이 무너지고
  오프라인에서 죽는다
- API 이음새 = `docs/database/mvp-erd.md` §9 문맥 조립. 백엔드가 검색의 권위, 로봇이 타이밍의 권위
- **프로필·복약·일정·회피주제를 벡터 검색하지 않는다.** "혈압약"과 "혈당약"은 임베딩상 거의 동일

### ai_chat 통합 (이번 구조 특유)

| 항목 | 결정 |
| --- | --- |
| `config.py` vs `policy.py` | 전자는 환경변수(배포마다 바뀜), 후자는 정책 상수(제품 판단이 바뀔 때). **합치지 않는다** |
| `pipeline.py` vs `graph/` | pipeline = 입력 루프 드라이버(캡처→STT→`app.invoke`), graph = 판단·생성, `emit` = 재생 시작(비블로킹) |
| **barge-in 전제** | pipeline이 재생 완료를 기다리면 barge-in이 원리적으로 불가능. 205번에서 확인 |
| `db/` vs `backend_client/` | 전자는 의료 참조 조회(병원·약국·의약품, 유지), 후자는 어르신의 사실·기억. `ssh_tunnel`로 기억을 직접 조회하지 않는다 |
| `llm/router.py` | 임베딩 기반 의도 라우팅이 이미 있다. 재구현 금지. 규칙으로 먼저 걸러내고 애매할 때만 위임(왕복 비용) |
| 핸들러 | 얇게. `llm/client.py`, `llm/medical_flow.py`, `weather/client.py`, `db/medical_repository.py`에 위임. HTTP·SQL 직접 사용 금지 |

### 안전과 프라이버시

- 티어: **T1** 즉시(동의 불필요) / **T2** 일일 요약(통보) / **T3** 동의 필요 / **T4** 절대 미전송
- **자해 암시는 동의를 무시하고 T1.** 단 로봇이 상담하지 않는다. 따뜻하게 반응하고 사람에게 넘긴다
- T1은 `guardian_sharing_consent_status`와 무관하게 전송(생명 안전). T2·T3는 동의 확인
- `fact_candidate` 확인(사실을 쓰기 전)과 T3 동의(감정을 공유할 허락)는 **다른 것.** 흐름 재사용 금지
- 보호자에게는 **집계와 이상치만.** 원본 이동 기록·대화 기록 금지
- 오탐을 값싸게: 119 직통이 아니라 보호자가 1초에 dismiss 가능한 알림으로

### 안전 신호는 3개뿐

음성 / 현관 센서 / rest_state. 침묵의 의미가 조합으로 갈린다.

| occupancy | rest_state | 의미 | 사다리 |
| --- | --- | --- | --- |
| HOME | AWAKE | **의심** | 가동·가속 |
| HOME | RESTING | 정상(낮잠) | 대기 |
| AWAY | any | 정상 | 정지 |
| UNKNOWN | any | 애매 | **보수적 가동** |

- 침묵을 **재지 말고 테스트한다.** 프로브 3단계 후 T1
- 오탐이 쌓이면 보호자가 알림을 안 읽고, 그때부터 진짜 응급을 놓친다.
  **시끄러운 감지기는 짜증이 아니라 안전 실패다**
- 현관 하트비트가 없으면 "아무도 안 움직임"과 "Pi가 죽음"을 구분할 수 없다
- Pi 타임스탬프는 참고용. **Jetson 도착 시각이 권위**(`clock.now()`로 정규화)

### 하드 규칙

1. `clock.py` 밖에서 `time.time()` / `datetime.now()` 호출 금지
2. 턴당 **생성 LLM 호출 1회.** 트리아지·인텐트·맞장구는 로컬 규칙
3. 임계치는 전부 `policy.py`. 함수 본문에 박지 않는다
4. 우선순위 동작 변경은 `PRIORITY_POLICY` / `PRIORITY_RANK` **표 수정**으로. 게이트 루프에 예외 추가 금지
5. 복약·건강 사실을 그대로 기록하지 않는다. `fact_candidate` 경유
6. 한 대화에 활성 `fact_candidate` 하나만 질의
7. 프롬프트는 `prompts/`에 파일로. `build_prompt()`는 그래프 없이 테스트되는 순수 함수
8. 회피 목록은 '정보'가 아니라 **금지문**으로 프롬프트에 넣는다
9. `emit`은 블로킹하지 않는다
10. 발신 큐(outbox)만 동기 쓰기. 나머지는 SD카드 수명을 위해 버퍼링·완화
11. **주석과 docstring은 한글.** 식별자·타입·로그는 영어

### 하드웨어 전제

- Jetson Orin Nano 8GB Dev Kit. 배터리 구동. 온보드 저장장치 없음(microSD)
- STT·TTS·LLM·임베딩이 **전부 외부 API** → GPU는 거의 유휴, 대신 **네트워크가 병목**
- 턴 예산 약 2초. 스트리밍 STT, 첫 문장부터 TTS 투입, 분류는 로컬
- 오프라인 대비 **필수 2건**: critical 프로브용 캐시 오디오, 알림 로컬 재시도 큐
- 성능 저하 순서: top-k 축소 → 문서 RAG 비활성 → ambient 중단 → 프로브 단순화.
  **안전 경로는 마지막까지 살린다**
- aarch64. `pip install`을 **실기에서** 확인(200번). x86 휠이 없으면 소스 빌드로 실패할 수 있다

---

## 5. 착수 전에 결정해야 하는 것 (막힘 요소)

| 항목 | 왜 막히나 | 관련 |
| --- | --- | --- |
| **`memory.visibility` 에 T4 값이 있는가** | 없으면 "로봇만 알고 보호자에게 절대 안 감"을 표현할 수 없고 → T3가 작동하지 않는다. 컬럼정의서 Excel 확인 | 201 |
| **임베딩 모델 차원** | `VECTOR` 컬럼은 DDL에 차원을 박아야 한다. Upstage 차원 확인 + pgvector HNSW 인덱스 상한과 대조 | 201 |
| **`.agents/skills/.../code-comment-policy.md` 와 `graph-workflows.md`** | CLAUDE.md §21·§6과 충돌할 수 있다. **규칙이 두 군데 있으면 에이전트가 어느 쪽을 따를지 알 수 없다.** 비교해서 하나로 통합 | 200 |
| T3 동의 큐 위치 | `care_record`에 pending 상태로 둘지, 전용 테이블을 만들지 | 211 |
| 일간 활동 지표 위치 | `care_record` 관찰 타입인지 전용 테이블인지 | 201, 211 |
| MQTT 토픽·페이로드·하트비트 주기 | `docs/mqtt/topic-convention.md`와 정렬 | 208 |
| occupancy: 이진 플래그 vs 인원 카운트 | 센서 정확도에 달림. 방문자 오판 대응 | 208 |
| 보호자 채널 (웹앱/푸시) | 인터페이스는 고정, 구현 미정 | 211 |
| AEC 적용 vs 임계치 완충만 | 실기 측정 후 결정 | 205 |
| 원거리 마이크 성능 | 소프트웨어로 못 고침. 일찍 확인 | 205 |

---

## 6. 이 대화에서 하지 못한 것 (오해 방지)

- **레포에 접근하지 못했다.** 파일을 직접 수정하거나 커밋·MR을 만들지 않았다.
  산출물은 전부 다운로드된 파일이다
- `.agents/skills/` 하위 문서를 읽지 못했다. §5의 충돌 확인이 필요한 이유
- Claude Code 의 IDE 확장 기능 범위를 확인하지 못했다.
  참고: `https://docs.claude.com/en/docs/claude-code/overview`
- 코드는 **전부 스텁**이다. 다음 4개 함수는 함정이 있어 **일부러** `NotImplementedError`로 남겼다.
  각 함수 docstring 의 "주의사항"을 구현 전에 읽어야 한다.

| 함수 | 함정 |
| --- | --- |
| `gate.is_quiet_hours` | 자정을 넘는 창(22:00~07:00) + UTC→로컬 변환. naive 비교는 가장 중요한 시간대에서 틀린다 |
| `jobs.ticks._is_absence_expected` | UNKNOWN을 '정상'으로 취급하면 죽은 현관 노드가 안전 감시를 조용히 끈다 |
| `triage._is_emergency` | 부정·시제("안 아파" / "어제 아팠어" / "아파") |
| `triage._is_self_harm` | 표현 목록을 즉흥적으로 만들지 말 것. 사람이 검토 |

나머지 미구현 지점은 `# TODO(모듈명):` 형태로 표시되어 있고, 어느 모듈이 채울지 적어두었다.

---

## 7. Claude Code 첫 세션 (그대로 사용 가능)

### 0단계: 파일 배치

`S15P11E102/` 에서 zip 을 그대로 풀면 §2 위치에 들어간다. 기존 파일과 겹치는 것은 없다.
풀고 나면 **커밋 전에** §5 의 세 번째 항목(`.agents` 문서와 CLAUDE.md 충돌 확인)을 먼저
해결한다. 규칙이 두 군데 있는 상태로 커밋하면 이후 모든 세션이 영향을 받는다.

### 1단계: 브랜치

GitLab 에서 **S15P11E102-200 이슈로부터 브랜치 생성** → 자동 네이밍을 그대로 사용.

### 2단계: 세션 시작 지시문

```
S15P11E102-200 작업을 시작한다.

먼저 읽어라:
1. 레포 루트 CLAUDE.md — 대화 런타임의 권위 문서
2. HANDOFF.md — 지금까지의 결정과 미결 사항
3. Jira S15P11E102-200 — 이 티켓의 작업 내용과 완료 조건
4. ai_chat/src/bomi_ai_chat/ 의 기존 구조

작업 원칙:
- 주석과 docstring은 한글로 쓴다. 식별자·타입·로그는 영어.
  형식은 CLAUDE.md §21 템플릿을 따른다.
- 기존 config.py 를 수정하지 말고 policy.py 를 따로 둔다. 성격이 다르다.
- 기존 llm/ stt/ tts/ weather/ db/ 모듈을 재구현하지 않는다. 위임한다.
- time.time() 을 clock.py 밖에서 호출하지 않는다.

완료 후에는 티켓의 완료 조건 각 항목을 어떻게 만족했는지 항목별로 보고하라.
만족하지 못한 항목은 만족했다고 하지 말고 그대로 보고하라.
```

### 3단계: 티켓별 세션 분리

**브랜치 하나 = 세션 하나.** 200번과 204번을 한 세션에서 하면 컨텍스트가 섞여 판단이 흐려진다.

### 사람이 반드시 검토해야 하는 티켓

| 티켓 | 이유 |
| --- | --- |
| 201 | **적용된 Flyway 마이그레이션을 수정하면 체크섬이 깨져 배포가 터진다.** 새 V파일만 추가하는지 확인 |
| 205 · 208 | 하드웨어 실기 검증이 필요. 에이전트가 완료를 판단할 수 없다 |
| 210 | 자해 판별 표현 목록은 LLM이 생성하게 두지 않는다 |

### 이 대화(웹)와 Claude Code 의 역할 분담

- **Claude Code** — 파일 수정, 리팩터링, 테스트 실행, 커밋·MR
- **웹 대화** — 설계 판단. 예: `memory.visibility` T4 결정, 부정·시제 규칙 설계,
  프롬프트 다듬기, diff 리뷰

---

## 8. 개발 순서 (CLAUDE.md §22 요약)

0. **가짜 시계 + `notify_guardian` 인터페이스.** 둘 다 나중에 넣기 어렵고, 뚫어두면 이후 전 단계
   테스트가 가능해진다. Jetson 실기 `pip install` 도 여기서 확인
1. **`localstore` + `backend_client`.** 모든 것이 이 둘을 통해 읽고 쓴다
2. **반응형 1왕복.** 기존 STT·TTS 연결. **턴 지연 실측**
3. **에코 억제.** 능동 발화 전에. 안 하면 이후 테스트가 전부 오염된다
4. **능동성.** 게이트 + 침묵 틱. 압축 시계로 검증
5. **현관 센서.** 게이트가 있어야 인사 제안을 붙일 수 있다. 이동 로직은 후순위
6. **계약 주도형 대화.** `fact_candidate` 흐름을 열어야 메모리 쓰기 경로가 완성된다
7. **트리아지·에스컬레이션 마지막.** 신호 튜닝이 가장 오래 걸린다

> 학부 프로젝트에서 가장 흔한 실패는 **반응형 기본 루프가 아직 깨진 상태에서 능동·안전을
> 먼저 붙이는 것**이다. 순서를 지킨다.

---

## 9. "자연스러운 대화"의 조작적 정의 (CLAUDE.md §17)

분위기가 아니라 검증 항목이다. 각 줄이 테스트다.

1. 짧은 턴 — 하나만 말하고 되묻는다. 정확한 긴 답변은 실패
2. 이어짐 — 어제 무릎 얘기, 손자 방문을 참조
3. 아는 걸 다시 묻지 않음
4. 반복에도 같은 온기 — 열 번째 "오늘 며칠이야?"가 첫 번째와 같은 어조
5. 회피 목록 준수 — 돌아가신 배우자를 살아있는 것처럼 언급하지 않음
6. 말 안 할 때를 안다
7. 나쁜 ASR을 견딤 — 자신 있게 틀린 답 대신 되묻는다
8. 표현 다양성 — 같은 알림이 3일 연속 똑같지 않음
9. 내부 기제 언급 금지 — "기록에 따르면", "AI로서", 티어·제안·검색 언급 없음
10. 회상이 작동

**지남력 반복 처리가 이 설계의 상징적 지점이다.** 반복 사실은 상태 로그에 기록해서
T2 추세로 보호자에게 올리지만, **절대 프롬프트에 닿게 하지 않는다.** 닿으면 어조에
새어나가 열 번째 답변이 짜증스럽게 들린다. 저장소를 나눠서 이 둘을 동시에 달성한다.
