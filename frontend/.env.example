# 운영(i15e102.p.ssafy.io)에서는 Nginx가 같은 출처의 /api와 /ws를 프록시합니다.
VITE_API_BASE_URL=/api
VITE_WS_URL=/ws
VITE_USE_MOCK_API=true
# 서버가 인증된 보호자-어르신 관계로 모든 Guardian API를 제한한 뒤에만 true로 전환합니다.
VITE_GUARDIAN_API_AUTH_READY=false
