# YDLIDAR X4-PRO 초기 실행 설정
#
# 현재 사용 중인 X4-PRO 실기기와 ROS 2 Humble 환경에서
# 정상적인 /scan 발행을 확인한 값이다.
#
# port:
#   기본 개발 환경에서는 /dev/ttyUSB0을 사용한다.
#   장치 번호가 달라지는 환경에서는 launch 인자 port:=... 로 덮어쓴다.
#
# baudrate:
#   현재 X4-PRO 연결에서 정상 통신을 확인한 값이다.
#
# sample_rate, range_min, range_max:
#   현재 센서 테스트에 사용하는 초기값이며,
#   실제 로봇 장착 및 장애물 감지 검증 후 조정할 수 있다.

ydlidar_ros2_driver_node:
  ros__parameters:
    port: /dev/ttyUSB0
    frame_id: laser_frame
    ignore_array: ""
    baudrate: 128000
    lidar_type: 1
    device_type: 0
    isSingleChannel: true
    intensity: false
    intensity_bit: 0
    sample_rate: 5
    abnormal_check_count: 4
    fixed_resolution: true
    reversion: false
    inverted: false
    auto_reconnect: true
    support_motor_dtr: false
    angle_max: 180.0
    angle_min: -180.0
    range_max: 10.0
    range_min: 0.12
    frequency: 10.0
    invalid_range_is_inf: false
    debug: false
