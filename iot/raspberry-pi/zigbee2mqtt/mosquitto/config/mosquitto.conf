listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout

# 장치 로컬의 conf.d/*.conf 만 추가로 읽는다. EC2 비밀번호가 포함된 실제
# bridge.conf 는 Git에 커밋하지 않는다.
include_dir /mosquitto/config/conf.d
